{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:ui": "turbo run build --filter=ui",
    "dev": "turbo run dev",
    "dev:apps": "turbo run dev --filter=docs --filte=web",
    "dev:cli": "turbo run dev --filter=chalk",
    "dev:ui": "turbo run dev --filter=ui --filter=workshop",
    "version-packages": "changeset version",
    "release": "turbo run build --filter ui --filter chalk && changeset publish"
  },
  "devDependencies": {
    "@headlessui/tailwindcss": "^0.1.3",
    "@tailwindcss/forms": "^0.5.3",
    "@tailwindcss/typography": "^0.5.9",
    "tailwindcss": "^3.3.2",
    "tsup": "^6.7.0",
    "turbo": "latest",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@changesets/changelog-github": "^0.4.8",
    "@changesets/cli": "^2.26.1"
  },
  "packageManager": "pnpm@7.15.0"
}
