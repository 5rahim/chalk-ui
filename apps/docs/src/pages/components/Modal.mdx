import {Modal}         from "ui";
import Preview         from "../../components/Preview";
import {useState}      from "react";
import {Button}        from "nextra/components";
import StorybookButton from "../../components/StorybookButton";

# Modal

<StorybookButton slug="components-overlays-modal--docs" />

export const Highlight = ({children}) => {

    const [test, setTest] = useState(false)

    return (
        <>
            <Button onClick={() => setTest(true)}>Open modal</Button>
            <Modal isOpen={test} onClose={() => setTest(false)} title="Modal" isClosable>
                Lorem ipsum dolor sit amet, consectetur adipisicing elit. A accusantium asperiores, blanditiis
                cupiditate delectus dolor enim eveniet fuga incidunt itaque laudantium non nostrum
                quaerat quis quod saepe sit ullam voluptate!
            </Modal>
        </>
    )
};

<Preview>
    <Highlight />
</Preview>

## Installation

```shell copy
npx @rahimstack/chalk-ui add modal
```

