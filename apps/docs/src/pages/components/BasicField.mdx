import Preview         from "../../components/Preview";
import {BasicField}    from "ui";
import StorybookButton from "../../components/StorybookButton";

# BasicField

`BasicField` is a primitive component upon which every form component is made.

<StorybookButton slug="components-forms-basicfield--docs" />

<Preview>
    <BasicField
        label="Field label"
        className={"w-full gap-1"}
        help="Help text"
        error="Error!"
    >
        <div>{`<Component/>`}</div>
    </BasicField>
</Preview>

```tsx
import {BasicField} from "@/components/ui/basic-field";

<BasicField
    label="Field label"
    className="w-full gap-1"
    help="Help text"
    error="Error!"
>
    <Component />
</BasicField>
```

## Installation

```shell copy
npx @rahimstack/chalk-ui add basic-field
```

