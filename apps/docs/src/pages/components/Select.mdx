import {Select}        from "ui";
import Preview         from "../../components/Preview";
import StorybookButton from "../../components/StorybookButton";

# Select

<StorybookButton slug="components-forms-select--docs" />

<Preview>
    <Select
        label="Country"
        placeholder="Select a country"
        options={[
            {value: 'ci', label: "Côte d'ivoire"},
            {value: 'tg', label: "Togo"},
        ]}
    />
</Preview>

## Installation

```shell copy
npx @rahimstack/chalk-ui add select
```

