import { Meta } from '@storybook/blocks';
import { APIButton } from "../components/APIButton";
import * as Stories from '../stories/PhoneNumberInput.stories';

<Meta of={Stories} name="README"/>

# PhoneNumberInput

An input that allows users to increment or decrement numeric values using the keyboard or pointer.

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add phone-number-input
```

## Reference

Knowing the dependencies can help you customize or extend the component with additional functionality.

<APIButton href={"https://www.npmjs.com/package/react-phone-number-input/v/3.2.23"}>react-phone-number-input</APIButton>
<APIButton href={"https://www.npmjs.com/package/libphonenumber-js/v/1.10.36"}>libphonenumber-js</APIButton>

## Family

This component also uses `BasicField`, `Input`, `TextInput`.
