import { Meta } from '@storybook/blocks';
import * as Stories from '../stories/Drawer.stories';

<Meta of={Stories} name="Info"/>

# Drawer

Display an overlay area.

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add modal
```

## Family

This component also uses `CloseButton`.

