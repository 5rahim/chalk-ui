import { Meta } from '@storybook/blocks';
import * as Stories from '../stories/LoadingOverlay.stories';

<Meta of={Stories} name="Info"/>

# LoadingOverlay

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add loading-spinner
```

