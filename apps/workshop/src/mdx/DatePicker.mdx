import { Meta } from '@storybook/blocks';
import { APIButton } from "../components/APIButton";
import * as Stories from '../stories/DatePicker.stories';

<Meta of={Stories} name="README"/>

# DatePicker

An input that combines a DateField and a Calendar popover to allow users to enter or select a date.

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add date-time
```

## Reference

Knowing the dependencies can help you customize or extend the component with additional functionality.

<APIButton href={"https://react-spectrum.adobe.com/react-aria/useDatePicker.html#usage"}>React Aria</APIButton>

## Family

This component also uses `BasicField`, `Input`, `Modal`.
