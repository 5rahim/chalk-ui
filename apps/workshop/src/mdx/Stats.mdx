import { Meta } from '@storybook/blocks';
import * as Stories from '../stories/Stats.stories';

<Meta of={Stories} name="README"/>

# Stats

<br/>

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add stats
```

## Family

This component also uses `ShowOnly`.
