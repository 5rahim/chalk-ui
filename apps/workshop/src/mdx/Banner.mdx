import { Meta } from '@storybook/blocks';
import * as Stories from '../stories/Banner.stories';

<Meta of={Stories} name="README"/>

# Banner

<br/>

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add banner
```

## Family

This component also uses `CloseButton`, `ShowOnly`.
