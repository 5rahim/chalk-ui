import { Meta } from '@storybook/blocks';
import { APIButton } from "../components/APIButton";
import * as Stories from '../stories/NumberInput.stories';

<Meta of={Stories} name="README"/>

# NumberInput

An input that allows users to increment or decrement numeric values using the keyboard or pointer.

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add number-input
```

## Reference

Knowing the dependencies can help you customize or extend the component with additional functionality.

<APIButton href={"https://zagjs.com/components/react/number-input#usage"}>Zag</APIButton>

## Family

This component also uses `BasicField`, `Input`, `TextInput`.
