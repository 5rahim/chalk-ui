import { Meta } from '@storybook/blocks';
import { APIButton } from "../components/APIButton";
import * as Stories from '../stories/TimeInput.stories';

<Meta of={Stories} name="README"/>

# TimeInput

An input that allows users to enter and edit time values.

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add date-time
```

## Reference

Knowing the dependencies can help you customize or extend the component with additional functionality.

<APIButton href={"https://react-spectrum.adobe.com/react-aria/useTimeField.html#usage"}>React Aria</APIButton>

## Family

This component also uses `BasicField`, `Input`.
