import { Meta } from '@storybook/blocks';
import * as Stories from '../stories/Pagination.stories';

<Meta of={Stories} name="Info"/>

# Pagination

Switch between pages.

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add pagination
```

## Usage

This component is purposefully non-reactive, allowing you to use it with multiple implementations.
