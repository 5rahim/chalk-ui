import { Meta } from '@storybook/blocks';
import { APIButton } from "../components/APIButton";
import * as Stories from '../stories/RadioGroup.stories';

<Meta of={Stories} name="Info"/>

# RadioGroup

A control that allows users to make a single choice from a select number of options.

## Installation

In order to install the component, run the following command:

```shell
npx @rahimstack/chalk-ui add radio-group
```

## Reference

Knowing the dependencies can help you customize or extend the component with additional functionality.

<APIButton href={"https://zagjs.com/components/react/radio-group#usage"}>Zag</APIButton>

## Family

This component also uses `BasicField`.
